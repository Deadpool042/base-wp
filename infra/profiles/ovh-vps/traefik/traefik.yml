entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false
  file:
    filename: /dynamic.yml
    watch: true

# ⚠️ En local on désactive ACME (Let’s Encrypt ne marchera pas sur basewp.local)
# (On le remettra en prod via un override compose.prod.yml ou traefik.prod.yml)
# certificatesResolvers:
#   letsencrypt:
#     acme:
#       email: "${TRAEFIK_EMAIL}"
#       storage: /acme.json
#       httpChallenge:
#         entryPoint: web

api:
  dashboard: false

log:
  level: DEBUG